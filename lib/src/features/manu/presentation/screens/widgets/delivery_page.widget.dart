import 'package:flutter/material.dart';

class DeliveryPageWidget extends StatelessWidget {
  const DeliveryPageWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Delivery Page'));
  }
}
